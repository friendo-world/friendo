{
  "name": "friendo-world-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "lsof -ti:8787 | xargs kill 2>/dev/null; wrangler dev",
    "sync": "bash sync.sh ../testsite",
    "sync:remote": "bash sync.sh --remote",
    "deploy": "wrangler deploy --env production",
    "schema": "bash sync.sh --schema-only"
  },
  "dependencies": {
    "better-auth": "^1.6.0",
    "hono": "^4.12.12",
    "kysely": "^0.28.15",
    "kysely-d1": "^0.4.0"
  }
}
