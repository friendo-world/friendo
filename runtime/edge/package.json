{
  "name": "friendo-edge-runtime",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8788",
    "schema": "wrangler d1 execute DB --local --file=schema.sql",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "hono": "^4.12.12"
  }
}
